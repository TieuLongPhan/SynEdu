version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    nodejs: "22"
  commands:
    - python -m pip install "uv==0.11.21"
    - uv --version
    - node --version
    - uv sync --frozen
    - uv run python --version
    - BASE_URL="/$READTHEDOCS_LANGUAGE/$READTHEDOCS_VERSION" uv run jupyter book build --execute --html --execute-parallel 1 --ci
    - uv run python scripts/inject_site_assets.py
    - test -f _build/html/index.html
    - mkdir -p "$READTHEDOCS_OUTPUT/html"
    - cp -r _build/html/. "$READTHEDOCS_OUTPUT/html/"
    - rm -rf _build/html
