# API

The SynEdu package is intentionally small: most content lives in notebooks, while the library provides helpers that keep workflows reproducible.

```{autosummary}
:toctree: _autosummary

synedu
synedu.cli
synedu.utils
```
